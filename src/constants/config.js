/**
 * App-wide constants. Nothing environment-specific is hardcoded elsewhere —
 * paths and keys that could change per-deployment live here.
 */
export const PREFS_STORAGE_KEY = 'hkj-prefs';
export const HEADER_OFFSET_PX = 116;
export const BACK_TO_TOP_THRESHOLD_PX = 480;
export const NAV_SUPPRESS_MS = 900;
